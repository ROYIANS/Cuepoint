# 小光点 Cuepoint

AI-assisted storyboard → video workflow — from 分镜 to 成片.

## Stack

Vite · React · TanStack Router · Dexie (IndexedDB)

## Commands

```bash
pnpm install
pnpm dev
pnpm test
pnpm lint
```
