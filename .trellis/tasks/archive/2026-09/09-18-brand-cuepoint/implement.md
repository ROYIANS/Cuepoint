# Implement — Brand Cuepoint

1. Update `index.html` title → 小光点.
2. Update `package.json` name → `cuepoint`.
3. Replace user-facing「爱分镜」in `src/lib/projectPackage.ts` (and any other `src/` hits).
4. Add sidebar/`aria-label` brand hint on studio mark if missing (小光点).
5. Update `.trellis/spec/frontend/index.md` product naming.
6. Add root `README.md` (小光点 Cuepoint + one-liner + stack).
7. `pnpm test` + `pnpm lint`.
8. `git remote add origin https://github.com/ROYIANS/Cuepoint.git` (if missing) and push `main` after commit.
