# Implement — AI connectors

1. Dexie: connectors/settings store + repo get/upsert/delete; confirm ZIP ignore.
2. `src/lib/ai/openaiCompatible.ts` — testConnection.
3. Catalog + Connectors page UI (studio route + shell nav).
4. Dialog install/edit + toast on test.
5. Optional DeepSeek catalog card with preset base URL (same form).
6. Unit test: URL normalize / header build; mock fetch test success path if easy.
7. Update `docs/product/ai-connectors.md` if needed.
8. `pnpm lint` && `pnpm test`.
