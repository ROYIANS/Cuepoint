# Design — 镜头状态与筛选

Add `status` on `Shot` and normalizers. Filter pure functions in a small helper used by editor and delivery. Missing first frame / clip derived from `result?.mediaId`. Persist filters on `ShotSettings`. Extend `episodeDelivery` CSV headers with 状态. Labels map in `domain/types` or `domain/columns`.
