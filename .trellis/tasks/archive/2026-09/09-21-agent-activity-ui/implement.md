# 执行计划

1. 在原保存工具轮事务记录公开过程；实现旧会话兼容及顺序分组，测试。
2. 重做可手动折叠的过程与实时用时，完成后收起。
3. 实现当前话题跨 run 待办提醒与直达入口，收紧 composer 条间距。
4. 类型检查、相关测试、完整测试与生产构建。
5. 独立 trellis-check 审阅并修复；隔离浏览器验证真实渲染、计时、顺序、反复折叠、完成收起、批次直达与390px布局。
6. 同步规范，提供中文结果和提交计划。未经本次明确提交指令先保留工作区。

回退：可选字段向后兼容，无schema迁移；各展示模块可以独立恢复原实现。

7. 用户追加轮次定位栏：实现分轮摘要与当前轮次选择辅助函数/测试、刻度/预览/跳转，浏览器验证多轮桌面/手机和手动阅读不被拉到底。
