# Implementation Plan — 分镜键盘与批量编辑

1. Add keyboard controller hook bound to shot editor.
2. Extend bulk toolbar with status, characters, scene, notes.
3. Repo helper for bulk patch selected shots if needed.
4. Tests for form-field gating and character replace semantics.
5. Manual shortcut matrix; `pnpm test` / `pnpm lint` / `pnpm build`.
