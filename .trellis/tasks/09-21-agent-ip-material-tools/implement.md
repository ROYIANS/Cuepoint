# Implementation plan

1. 固定当前44/50工具、技能和schema估算基线与普通/任务/项目上下文测试样例。
2. 做运行时工具发现和加载状态；同步两协议请求、预算、Inspector、压缩、resume与旧run兼容。先证明无法加载禁用能力、不能在同轮使用刚发现尚未提供schema的工具。
3. 接入IP读写、可读创建确认、项目关联和有界IP上下文，测试创建只一次、拒绝/取消不落库、CAS与归属变化。
4. 接入素材读写、显式版本/归属/来源和采用，接真实像素及受限文档分页，测试二进制不进入tool JSON、未知/跨归属/历史版本不被误用。
5. 量化首轮及典型任务总开销；跨领域任务发现、批准、拒绝、刷新、续跑、复杂任务切换验证；现有全部业务回归。
6. 本机pnpm lint、定向测试、全量测试、生产构建、隔离浏览器验证；独立审查后更新规范/中文报告。用户已确认，任务已启动，产品实现及验证记录见 report.md。

Commands use /Users/xiaomengdao/.nvm/versions/node/v24.11.0/bin/pnpm explicitly. 不自动提交/归档，不调用真实付费服务。
