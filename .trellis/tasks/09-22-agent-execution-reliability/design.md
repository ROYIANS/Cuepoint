# Execution readiness and truthful stop presentation

User approved the initiative and starting Agent flow work on 2026-09-22. This child owns R1; R2 evidence validation and batch tools remain separate.

## Boundary
Retain the pending authorized project-kind preload and shared execution instructions. Add deterministic execution presentation from owned run/call ledger only. Completed response != completed business work. Expose no-call, bookkeeping-only, read-only, completed calls, failed/rejected calls, approval, unknown effect, budget pause, interruption, failure and cancellation. Do not infer intent, blocker category or success from prose or arbitrary error keywords. Legacy contradictory records remain unverified.

Pure helper `describeRunExecution(run,calls)` returns kind/label/detail and bounded diagnostic counts. UI replaces the current ad-hoc summary label and adds details inside the existing process disclosure. Use existing Button for touched summary control, normal shadcn appearance, no decorative border or extra card. Diagnostics expose actual step/tool counts, protocol and latest offered count when known, not raw prompts/credentials/reasoning. Completed tool calls do not prove saved media; copy explicitly avoids a business completion claim.

No forced no-call requests or new paid calls. Recovery controls and permission rules remain unchanged; no new generic Continue action on completed runs. Existing frozen input/retry and conversation mode contracts stand.

## Evidence limits
Original provider trace unavailable through current browser tooling. Mock both protocols through the actual runtime and verify the no-call end and subsequent tool-driven flow. Live-provider and browser checks are separate gates; do not mark the child fully accepted without that evidence.
