# Execution readiness and truthful stop presentation

User approved the initiative and starting Agent flow work on 2026-09-22. This child owns R1; R2 evidence validation and batch tools remain separate.

## Boundary
Retain the pending authorized project-kind preload and shared execution instructions. Add deterministic execution presentation from owned run/call ledger only. Completed response != completed business work. Expose no-call, bookkeeping-only, read-only, completed calls, failed/rejected calls, approval, unknown effect, budget pause, interruption, failure and cancellation. Do not infer intent, blocker category or success from prose or arbitrary error keywords. Legacy contradictory records remain unverified.

Pure helper `describeRunExecution(run,calls)` returns kind/label/detail and bounded diagnostic counts. UI replaces the current ad-hoc summary label and adds details inside the existing process disclosure. Use existing Button for touched summary control, normal shadcn appearance, no decorative border or extra card. Diagnostics expose actual step/tool counts, protocol and latest offered count when known, not raw prompts/credentials/reasoning. Completed tool calls do not prove saved media; copy explicitly avoids a business completion claim.

No forced no-call requests or new paid calls. Recovery controls and permission rules remain unchanged; no new generic Continue action on completed runs. Existing frozen input/retry and conversation mode contracts stand.

## Evidence limits
Original provider trace unavailable through current browser tooling. Mock both protocols through the actual runtime and verify the no-call end and subsequent tool-driven flow. Live-provider and browser checks are separate gates; do not mark the child fully accepted without that evidence.

## Second delivery — creation continuity (approved continuation)
Confirmed code gap: project_create always requires a fresh project conversation; audio/music editing cannot continue. No-call responses still end a model turn; do not implement keyword-based automatic retries without intent/stop evidence. This batch fixes the deterministic creation barrier and exercises continuous tool execution under both protocols.

Add optional `continueInProject` to project_create (default true): creation preview explicitly includes binding this conversation, false retains create-only/multiple-project behavior. In the existing atomic write transaction, create the project/seeds, bind previously unbound ordinary smart thread and current run once, save code-owned provenance (projectId/callId) on run, and persist receipt/result together. Existing bound threads never switch; ordinary bindChatThreadProject remains before-first-message only. Change preview revision to reject approvals prepared under old create-only semantics. Atomic replay returns already committed result without rebinding, but still validates owner/status and project existence.

Keep original requestMessages/old runs/step offers immutable. At the next settled boundary existing project and memory refresh inject current scope. Preload only the matching already-enabled sound group without expanding enabled capabilities; frozen skill instructions must explain continuation. Execution must re-read durable scope before subsequent calls in the same envelope, so pre-binding approvals cannot mutate an unrelated project. Stops, ask approval, paid review, unknown effects and step budget remain intact.

UI uses durable thread/run origin to show that creation continues here instead of offering a redundant new-chat button. Retain legacy/create-only new-chat behavior. Write-outcome projection recognizes the exact origin call and matching project seeds after binding; arbitrary bound project_create receipts remain invalid.

Files: businessTools/domain + narrow db binding helper; runChat/toolLoading/skills for durable current scope; runWriteOutcomes for provenance; CreatedEntityLinks for current-thread display; protocol/repository regressions. No provider changes, no live production credentials or paid generation; live-model acceptance remains separate if no isolated configured connector is available.

Creation continuity preconditions also reject active scope-bound references and preceding foreign business writes before creating any project. Their existing ownership cannot be silently relabeled. The explicit create-only option preserves the manual handoff route for these cases. Current browser access on this turn returns an authentication-unavailable error; live-model configuration cannot be verified through that surface. Do not inspect private browser stores or substitute production credentials.
