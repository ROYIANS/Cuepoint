# Tools and permissions

## Scope
Depends on execution/message foundation. Implement validated tool registration, multi-step calls, durable call results and pending approvals, three approved permission modes, builtin skill assembly and deterministic task/read-only fixture tools. Preserve approval after reload; deduplicate completed calls; validate scope/revision even in full access. Do not wire business CRUD, media or search providers yet.

## Acceptance
Verify persistence/reload, shared identity and every transition across chat/runtime. Existing conversations and project data must remain usable.

## Planning boundary
This child stays in planning until the upstream APIs are verified. The overall scope and sequential implementation are user-approved.
