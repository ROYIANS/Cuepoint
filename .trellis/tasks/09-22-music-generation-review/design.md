# Boundary and contracts
Gap: music approval currently displays JSON truncated to 1600 characters and generic approve; existing preflight already binds draft/connector revision. Preserve it and add optional typed music review snapshot to AgentToolPreview, validated at persistence. Snapshot version 1 contains projectId/projectName, draftId/draftRevision, connectorId/connectorLabel and complete MusicSettings. No credentials or endpoint. Full snapshot cannot silently truncate; accept bounded supported music settings with explicit preview size limit (up to 128 KiB for music only after strict validation; other previews retain existing limit).

Read-only review uses original snapshot plus shared strict parser, and code checks original args and snapshot against freshly prepared preview in a consistent DB transaction. Changed draft/connector/scope marks stale/unavailable; old saved review stays visible. Approval helper rechecks and uses existing durable resolveAgentToolApproval within the transaction; no automatic refresh to new version. Original execution preflight remains. Legacy snapshot absence displays clear incomplete-review state and cancellation, never generic approval fallback for music.

UI: short title, project, engine/mode, draft version and duration/vocals. Primary description/lyrics preview with existing shadcn Button to expand complete text, supported parameter details collapsed. Unknown charges disclosed as service-provider billing. No editable form; adjust the existing workspace draft then cancel/reprepare. Model parameters shown reflect musicWireInput; Simple ignores saved custom fields and makes that explicit. Active confirm checks readiness; history uses frozen contents. Use one panel with typography/spacing and default primitive styling.

Ownership: implement agent owns domain snapshot/preview production/validation/current-review reader + atomic approval and backend tests. Root owns pure display projection, MusicGenerationReview component, AgentRunDetails integration, display tests and task/spec docs. Independent checker reviews all code. No schema migration, network research or extra paid requests needed; existing local adapter is the scoped contract.
