# Optional public analytics configuration, embedded at build time.
# Copy to .env.local for local production builds. Development does not track.
# Use the src and data-website-id from your existing Umami tracking code.
# Leave either value empty to disable analytics.
VITE_UMAMI_SCRIPT_URL=
VITE_UMAMI_WEBSITE_ID=
